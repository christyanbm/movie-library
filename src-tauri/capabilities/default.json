{
  "identifier": "default",
  "description": "Default capabilities for Movie Library",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
